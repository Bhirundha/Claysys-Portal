class Employee{
  int leave_data;
  int emp_data;
  int approve_data;
  int pending_data;

  Employee({required this.leave_data,required this.emp_data,required this.approve_data,required this.pending_data});
}